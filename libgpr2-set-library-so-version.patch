--- a/gpr2.gpr
+++ b/gpr2.gpr
@@ -30,6 +30,7 @@
    for Object_Dir use Build_Root & "/" & Build & "/obj-" & Library_Type;
    for Library_Dir use Build_Root & "/" & Build & "/lib-" & Library_Type;
    for Library_Kind use Library_Type;
+   for Library_Version use "libgpr2.so." & external("VERSION");
 
    --------------
    -- Compiler --
